#include "main.h"
#include <stdio.h>

/**
 * print_rev - prints a string in reverse mode
 * @str: The string to be printed
 *
 * Return: void
 */
void print_rev(str)
{
	int i;

	i = strlen(str) - 1;

	for (; *str != *s[0]; s--)
	{
		_putchar(str);
	}
}

